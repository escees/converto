{
  "identifier": "default",
  "description": "Default capabilities for Converto",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default",
    "shell:allow-open"
  ]
}
